# 示波器倍频实验计算核心 PRD 文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 电光调制示波器波形计算核心 |
| 版本 | v1.0 |
| 创建日期 | 2026-03-29 |
| 所属场景 | 新场景（示波器倍频现象实验记录，尚未创建） |
| 范围 | 仅计算核心，不含场景搭建与 UI 渲染 |

---

## 1. 项目背景

### 1.1 实验原理

电光调制实验中，对晶体施加交流调制电压会改变其折射率，导致通过检偏器后的光强发生周期性变化。示波器同时显示调制电压波形和输出光强波形。

**倍频现象**：当静态工作点处于传递函数的极值处（Γ₀ = 0 或 mπ）时，输出光强的频率为输入电压频率的两倍。

### 1.2 目标

构建一个独立的计算核心模块，根据输入的电压、晶体参数等数据，实时计算并输出示波器的两条波形数据（float 数组），供新场景中的 UI 渲染。

---

## 2. 物理模型

### 2.1 电压模型

施加在晶体上的总电压：

```
V(t) = V_DC + V_m × sin(2πft)
```

- `V_DC`：直流偏置电压
- `V_m`：交流调制电压幅值
- `f`：交流调制频率

### 2.2 半波电压 Vπ

复用 `LabController` 中的计算逻辑，依赖 `CrystalPhysicalCore.Sensitivity`：

- **横向调制**：`Vπ = (λ × d) / (2 × L × S_eff)`
- **纵向调制**：`Vπ = λ / (2 × S_eff)`

其中：
- `λ`：激光波长（从 CrystalProfile 获取）
- `L`：晶体长度（mm → m）
- `d`：晶体厚度（mm → m）
- `S_eff`：有效电光灵敏度（从 `CrystalPhysicalCore.Sensitivity` 获取）

### 2.3 相位延迟

```
Γ(t) = π × V(t) / Vπ + δ_compensator
     = π × (V_DC + V_m × sin(2πft)) / Vπ + δ_compensator
```

- 静态部分：`Γ₀ = π × V_DC / Vπ + δ_compensator`
- 动态部分：`ΔΓ(t) = π × V_m × sin(2πft) / Vπ`

### 2.4 输出光强

```
I(t) = I₀ × sin²(Γ(t) / 2)
```

### 2.5 静态偏置 Γ₀ 的控制

- **与场景2完全解耦**，不依赖场景2中的任何状态
- **唯一控制手段**：DC 偏置电压 `V_DC`
- **不考虑**自然双折射（晶体角度默认 0°，光沿光轴传播）
- **预留**补偿器相移 `δ_compensator`（默认 0，后续按需启用）

### 2.6 倍频条件

| 工作点 | Γ₀ 值 | 输出特征 |
|--------|--------|----------|
| 极值点 | 0, mπ | 光强频率 = 2f（倍频） |
| 线性点 | (m+½)π | 光强频率 = f（同频） |

---

## 3. 输入参数

### 3.1 外部引用

| 参数 | 类型 | 来源 | 说明 |
|------|------|------|------|
| `CrystalPhysicalCore` | 组件引用 | 新场景提供 | 用于获取 Sensitivity 计算 Vπ |
| `CrystalProfile` | ScriptableObject | `CrystalSelectionData.SelectedProfile` | 晶体类型（Scene2-pre 中选定） |

### 3.2 可调参数

| 参数 | 类型 | 范围/默认值 | 说明 |
|------|------|-------------|------|
| `V_DC` | float | 由调度脚本控制 | DC 偏置电压（V） |
| `V_m` | float | 由调度脚本控制 | AC 调制电压幅值（V） |
| `frequency` | float | 由调度脚本控制 | AC 调制频率（Hz） |
| `modulationMode` | ModulationMode | Transverse / Longitudinal | 调制模式 |
| `fieldAxis` | ElectricFieldAxis | X / Y / Z | 电场轴方向 |
| `compensatorPhase` | float | 默认 0 | 补偿器相移（rad），预留 |
| `I₀` | float | 默认 1.0 | 最大光强（UI 渲染时归一化） |

### 3.3 波形配置参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `sampleCount` | int | 采样点数，可配置 |
| `displayPeriods` | float | 显示周期数，可配置 |

### 3.4 固定参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 晶体 XY 旋转 | (0°, 0°) | 理想默认值，不可调 |
| 波长 | 从 CrystalProfile 获取 | — |
| 晶体尺寸 | 从 CrystalProfile 默认值获取 | length_mm, thickness_mm |

---

## 4. 输出定义

### 4.1 Ch1 — AC 电压波形

```
Ch1[i] = V_m × sin(2π × f × t_i)
```

- 类型：`float[]`，长度 = `sampleCount`
- 仅包含 AC 分量，不含 V_DC
- 时间轴：`t_i = i × (displayPeriods / f) / sampleCount`

### 4.2 Ch2 — 输出光强波形

```
Ch2[i] = I₀ × sin²(Γ(t_i) / 2)
```

- 类型：`float[]`，长度 = `sampleCount`
- Γ(t_i) 包含完整的 V_DC + AC + compensator 贡献
- UI 渲染时自行归一化显示

### 4.3 辅助输出（建议）

| 输出 | 类型 | 说明 |
|------|------|------|
| `Vpi` | float | 当前计算的半波电压，供 UI 显示 |
| `Gamma0` | float | 当前静态相位偏置，供调试/UI |

---

## 5. 更新策略

- **触发条件**：任意输入参数变化时
- **更新粒度**：每帧重新计算整条波形（全量刷新，无滚动）
- **性能考量**：纯 CPU 浮点运算，采样点数可控，预期无性能瓶颈

---

## 6. 设计约束

1. **解耦原则**：遵循项目开发规范，不修改任何现有代码，使用独立模块
2. **与场景2解耦**：不依赖场景2的运行时状态，Γ₀ 完全在新场景内独立设定
3. **职责单一**：计算核心只负责数值计算和数组输出，不负责 UI 渲染
4. **代码复用**：Vπ 计算逻辑复用 `LabController` 中的公式（非引用，而是相同算法）
5. **可扩展**：补偿器相移预留接口，后续可启用

---

## 7. 依赖关系

```
CrystalSelectionData.SelectedProfile ──→ 计算核心 ──→ float[] Ch1 (AC电压)
CrystalPhysicalCore.Sensitivity     ──→            ──→ float[] Ch2 (光强)
场景调度脚本 (V_DC, V_m, f, ...)   ──→            ──→ float   Vpi
                                                    ──→ float   Gamma0
```

---

## 8. 验收标准

1. **倍频验证**：V_DC = 0（Γ₀ = 0）时，Ch2 波形频率应为 Ch1 的 2 倍
2. **同频验证**：V_DC = Vπ/2（Γ₀ = π/2）时，Ch2 波形频率应与 Ch1 相同
3. **消光验证**：V_DC = 0, V_m = 0 时，Ch2 应为全零（或接近零）
4. **Vπ 一致性**：相同晶体参数下，计算核心输出的 Vπ 应与 LabController 一致
5. **实时性**：参数变化后下一帧即反映在输出数组中
